# Local firmware secrets (tracked template: config.h.example)
config.h

# Generated factory bundles (serials + QR PNGs) — copy out of repo for production use
factory_serial_exports/output_*/

# Traefik ACME account data (Let's Encrypt)
croc_sentinel_systems/traefik/acme.json

# MQTT/TLS private keys — generate per environment (see croc_sentinel_systems/certs b1/README.md)
croc_sentinel_systems/certs b1/*.key

# Never commit RSA private key used for dashboard password recovery
password_recovery_keys/private.pem
