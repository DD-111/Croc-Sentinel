#ifndef BURN_SENTINEL_SERIAL_CONFIG_H
#define BURN_SENTINEL_SERIAL_CONFIG_H
// Standalone stub: Arduino only sees files in this sketch folder.
// MUST stay in sync with repo root `config.h` / `config.h.example`:
//   #define NVS_NAMESPACE "..."
// If production uses a non-default namespace, edit this line to match before burning.
#define NVS_NAMESPACE "sentinel"
#endif
